import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#F0C041",
    },
    common: {
      black: "#212121",
      white: "#F2F2F2",
    },
  },
  typography: {
    htmlFontSize: 16,
  },
});
